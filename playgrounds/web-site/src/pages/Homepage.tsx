import * as React from 'react'

const Homepage = () => {
  const title = '<TinyCore />'
  return (
    <div style={{ height: '460px', backgroundColor: '#fff', textAlign: 'center', lineHeight: '460px' }}>
      {title}
    </div>
  )
}

export default Homepage
